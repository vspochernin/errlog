# В случае, если оба контура на одной машине:
ERRLOG_KAFKA_EXTERNAL_HOST=host.docker.internal

# Если demo и core контуры будут на разных машинах, нужно заменить значение на IP или DNS core-машины.
# Например: ERRLOG_KAFKA_EXTERNAL_HOST=192.168.1.50
